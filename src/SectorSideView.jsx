import { observer } from 'mobx-react-lite';
import * as React from 'react';
import { Button } from 'react-bootstrap';

export default observer(function Sectors3DView() {
  return (
    <Button>This is the 3D view</Button>
  );
});
